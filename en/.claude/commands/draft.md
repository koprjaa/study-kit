---
description: Block C — draft the text section by section (phase 3.3)
---
Run phase 3.3 per CLAUDE.md.

From the artifacts (`outline.md`, `evidence.md`, `transitions.md`, `intro-drafts.md`) write the
study section by section into `study/` (`01-intro.md`, `02-...md`, …). Keep to the planned
length. Clean academic prose with footnotes; no meta-comments in `study/`.

IMPORTANT: before writing each quote, VERIFY it verbatim against `raw_primary/`. Use nothing you
can't find in `raw_primary/`. Log after each section. When done, propose `/diagnose`.
