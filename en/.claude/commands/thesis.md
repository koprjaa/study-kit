---
description: Block A — crystallize and stress-test the thesis (phases 1.1 + 1.2)
---
Run BLOCK A per CLAUDE.md, section "Phases → BLOCK A".

First read `idea.md` and search `raw_primary/` to see whether the insight has support in the corpus.

1.1: apply the thesis-ness test (5 conditions) and present 5 reformulations by risk (A–E).
Recommend a version for the target journal, but LEAVE THE CHOICE TO THE HUMAN — ask and wait.
1.2: on the chosen thesis run the 4 stress tests (scope, structurability, "so what", uniqueness),
plus the groundability check (is the load-bearing vocabulary the text's own or an imported label?).

GATE: the thesis meets the 5 thesis-ness conditions and all the tests. If not, go back.
Once it passes, write `process/thesis.md`, add a line to `log.md`, and propose `/outline`.
