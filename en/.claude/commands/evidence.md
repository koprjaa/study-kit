---
description: Block B/C — evidence selection from raw_primary/ + analytical depth (phases 2.4 + 3.1)
---
Run phase 2.4 and the following 3.1 per CLAUDE.md.

2.4: for each section SEARCH `raw_primary/` and find 4–6 candidate passages WITH PROVENANCE
(file + location, quote verbatim). Score 1–5 (relevance, persuasiveness, representativeness,
multifunctionality, independence), pick the main + supporting evidence.
RED FLAG: no passage > 3 → section unsupportable. NEVER supply an invented quote —
report that the corpus gives no support.

3.1: for each key quote run the depth work (paraphrase, link to thesis, layering ≥2 of 5,
transition, model paragraph, and 2–3 paragraph development for load-bearing quotes: what it
means / what it doesn't / the objection). Write/extend `process/evidence.md`, log it, propose
`/transitions`.
